defaults:
  - logger: logger
  - arch: anakin
  - system: ppo/ff_ppo_central_factored_tabular
  - network: mlp  # [mlp, continuous_mlp, cnn]
  - env: generalised-matrax # [cleaner, connector, gigastep, lbf, mabrax, matrax, rware, smax]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
